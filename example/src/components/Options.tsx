import React from 'react';
import Container from '@material-ui/core/Container';
import Box from '@material-ui/core/Box';

interface Props {}

const Options: React.FC<Props> = () => {
  return (
    <Container>
      <Box>Hello React Falling Emojis</Box>
    </Container>
  );
};

export default Options;
