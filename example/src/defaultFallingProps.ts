const defaultFallingProps = {
  disable: false,
  shake: false,
  reverse: false,
  repeat: -1,
  density: 1,
  speed: 10,
  size: 30,
  opacity: 1
};

const { disable: something } = defaultFallingProps;

console.log(something);

export default defaultFallingProps;
