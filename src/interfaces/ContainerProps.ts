export default interface ContainerProps {
  id: string;
  emoji: string;
  size: number;
  speed: number;
  disable: boolean;
  shake: boolean;
  reverse: boolean;
  repeat: number;
  opacity: number;
}
